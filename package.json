{
  "name": "NordisPaymentAPI-EBO",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio-run": "^2.6.0",
    "axios": "^0.21.0",
    "dateformat": ""
  },
  "devDependencies": {
    "twilio-run": "^2.6.0",
    "axios": "^0.21.0",
    "dateformat": ""
  },
  "engines": {
    "node": "12"
  }
}
